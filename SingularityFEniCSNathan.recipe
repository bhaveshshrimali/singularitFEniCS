Bootstrap: docker
From: quay.io/fenicsproject/stable:current

%post
    apt-get update && add-apt-repository ppa:mc3man/trusty-media && apt-get update && apt-get install ffmpeg && apt-get install frei0r-plugins && apt-get -y install python3-tk python3-pip && pip3 install ffmpeg-python
    ldconfig

%files
    WELCOME /usr/local/share/WELCOME

%runscript
    cat /usr/local/share/WELCOME 
    exec /bin/bash -i
