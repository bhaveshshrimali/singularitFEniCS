Bootstrap: docker
From: dolfinx/real:latest

%post
    apt-get -y update
    python3 -m pip install meshio, pandas, openpyxl 
    python3 -m pip install lxml
    ldconfig

%runscript
    exec /bin/bash -i